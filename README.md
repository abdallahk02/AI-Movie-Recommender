# AI-Movie-Recommender
Uses FAISS (Facebook AI Similarity Search) to find similar movies to a given title. Includes fuzzy matching for title searching as well as GUI implmented with PyQT. Embeddings are too large to upload on here, but can be generated using the generate_embeddings function.
